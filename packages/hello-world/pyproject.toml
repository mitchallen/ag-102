[project]
name = "hello-world"
version = "0.1.0"
description = "A simple hello world package"
requires-python = ">=3.8"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"
